import { ScreenPlaceholder } from "../../src/components/navigation/ScreenPlaceholder";

export default function AudioPlayerRoute() {
  return (
    <ScreenPlaceholder
      title="Audio Player"
      description="Stack route for future text-to-speech playback of scan explanations."
    />
  );
}
