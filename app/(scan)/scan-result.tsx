import { ScreenPlaceholder } from "../../src/components/navigation/ScreenPlaceholder";

export default function ScanResultRoute() {
  return (
    <ScreenPlaceholder
      title="Scan Result"
      description="Stack route for presenting extracted text, summaries, and suggested actions."
    />
  );
}
