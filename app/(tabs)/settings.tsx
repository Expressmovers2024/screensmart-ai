import { ScreenPlaceholder } from "../../src/components/navigation/ScreenPlaceholder";

export default function SettingsRoute() {
  return (
    <ScreenPlaceholder
      title="Settings"
      description="Tab route for accessibility, account, AI, voice, and storage configuration."
    />
  );
}
