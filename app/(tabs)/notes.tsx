import { ScreenPlaceholder } from "../../src/components/navigation/ScreenPlaceholder";

export default function NotesRoute() {
  return (
    <ScreenPlaceholder
      title="Notes"
      description="Tab route for user notes, follow-ups, and saved plain-language explanations."
    />
  );
}
