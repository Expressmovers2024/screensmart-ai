import { ScreenPlaceholder } from "../../src/components/navigation/ScreenPlaceholder";

export default function LibraryRoute() {
  return (
    <ScreenPlaceholder
      title="Library"
      description="Tab route for saved scans, summaries, and future Supabase-backed history."
    />
  );
}
